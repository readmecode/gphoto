# Google Drive → Google Photos Sync Configuration
# Copy this file to .env and update with your values

# ============================================
# Required Configuration
# ============================================

# rclone remote names (as configured with 'rclone config')
GDRIVE_REMOTE=gdrive
GPHOTOS_REMOTE=gphotos

# Source folder in Google Drive where photos are stored
SOURCE_PATH=Photo

# ============================================
# Optional: Quota Tracking (Recommended)
# ============================================

# Google Cloud Project ID for automatic quota synchronization via Monitoring API
# Set this to enable real-time quota tracking from Google Cloud Monitoring
# Requires: gcloud auth application-default login
#          gcloud services enable monitoring.googleapis.com
GOOGLE_CLOUD_PROJECT_ID=

# ============================================
# Optional: Advanced Settings
# ============================================

# Log directory (default: ~/gphoto_logs)
# LOG_DIR=~/gphoto_logs

# Maximum parallel uploads (default: 2)
# MAX_PARALLEL_UPLOADS=2

# Upload timeout in seconds (default: 600)
# UPLOAD_TIMEOUT=600

# Photo file extensions (comma-separated, default: .jpg,.jpeg,.png,.heic,.cr2)
# PHOTO_EXT=.jpg,.jpeg,.png,.heic,.cr2

# Video file extensions (comma-separated, default: .mp4,.mov,.avi,.mkv)
# VIDEO_EXT=.mp4,.mov,.avi,.mkv

# Ignored file extensions (comma-separated, default: .thm,.lrv,.json)
# IGNORED_EXT=.thm,.lrv,.json
