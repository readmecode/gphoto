# Google Drive and Google Photos rclone remote names
# Configure these using: rclone config
GDRIVE_REMOTE=gdrive
GPHOTOS_REMOTE=gphotos

# Source path in Google Drive to sync from
SOURCE_PATH=Photo

# Directory for logs and reports
LOG_DIR=~/gphoto_logs

# Maximum parallel uploads
MAX_PARALLEL_UPLOADS=2

# Upload timeout in seconds
UPLOAD_TIMEOUT=600

# Photo file extensions (comma-separated, no spaces)
PHOTO_EXT=.jpg,.jpeg,.png,.heic,.cr2

# Video file extensions (comma-separated, no spaces)
VIDEO_EXT=.mp4,.mov,.avi,.mkv

# File extensions to ignore (comma-separated, no spaces)
IGNORED_EXT=.thm,.lrv,.json
